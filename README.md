# TV Downloader - Загрузчик видео из Telegram каналов

Автоматизированный бот для массовой загрузки видеофайлов из Telegram каналов с поддержкой параллельных загрузок, умным извлечением названий и автоматическим выбором качества.

## 🎯 Основные возможности

### 📥 Загрузка видео
- ✅ **Массовая загрузка** из одного или нескольких каналов
- ✅ **Параллельная загрузка** нескольких файлов одновременно (настраивается)
- ✅ **Автоматический выбор качества** видео (360p, 480p, 720p)
- ✅ **Продолжение с места остановки** - метаданные сохраняются сразу после начала загрузки
- ✅ **Проверка дубликатов** - не скачивает уже загруженные файлы
- ✅ **Обработка ошибок** с автоматическими повторными попытками

### 📁 Организация файлов
- ✅ **Умное извлечение названий** из текста поста (не из имени файла)
- ✅ **Поддержка русских и английских названий** - автоматически выбирает оптимальный вариант
- ✅ **Структурированная организация** по каналам и сериям
- ✅ **Автоматическое создание папок** для каждой серии
- ✅ **Скачивание дополнительных материалов**:
  - Постеры (poster.jpg)
  - Дополнительные фото из альбомов (image_1.jpg, image_2.jpg...)
  - Описание (description.txt)

### 🔄 Работа с альбомами
- ✅ **Обработка медиа-альбомов** - корректно извлекает текст из всех сообщений альбома
- ✅ **Скачивание всех фото** из альбома вместе с видео
- ✅ **Умное определение названия** - ищет текст поста во всех сообщениях альбома

### 📊 Мониторинг и уведомления
- ✅ **Прогресс-бары** для каждой загрузки с отображением скорости
- ✅ **Детальное логирование** в консоль и файл
- ✅ **Уведомления в Telegram** о прогрессе и завершении (опционально)
- ✅ **Статистика загрузок** - количество скачанных, пропущенных, ошибок

### 🔐 Безопасность и надежность
- ✅ **Сохранение метаданных** сразу после начала загрузки
- ✅ **Проверка целостности файлов** - автоматически перезагружает неполные файлы
- ✅ **Обработка FloodWait** - автоматическое ожидание при ограничениях Telegram
- ✅ **Поддержка всех типов каналов**:
  - Публичные каналы (@channel_name)
  - Приватные каналы (invite links)
  - Каналы по ID

## 📋 Требования

- **Python 3.8** или выше
- **Telegram API credentials** (API ID и API Hash)
- **Токен Telegram бота** для уведомлений (опционально)
- **Интернет-соединение** для загрузки файлов

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Скопируйте `config/config.example.yaml` в `config/config.yaml` и заполните:

```yaml
telegram:
  api_id: YOUR_API_ID
  api_hash: YOUR_API_HASH
  channels:
    - "@channel_name"
    # или
    - "https://t.me/joinchat/..."
```

### 3. Первый запуск

```bash
python run.py
```

При первом запуске потребуется:
1. Ввести номер телефона (с кодом страны, например: +79991234567)
2. Ввести код подтверждения из Telegram

После этого сессия сохранится и код больше не понадобится.

## 📖 Подробная документация

- **[INSTALLATION.md](INSTALLATION.md)** - Подробная инструкция по установке и запуску для нового пользователя
- **[CONFIG_GUIDE.md](CONFIG_GUIDE.md)** - Руководство по настройке конфига для оптимизации скорости скачивания

## 📁 Структура проекта

```
TVDownloader/
├── config/
│   ├── config.example.yaml    # Пример конфигурации
│   └── config.yaml             # Ваша конфигурация (не в git)
├── src/
│   ├── __init__.py
│   ├── telegram_client.py      # Клиент Telegram API
│   ├── download_manager.py     # Менеджер загрузок
│   ├── file_handler.py         # Обработка файлов и метаданных
│   ├── video_quality.py        # Выбор качества видео
│   ├── notifier.py             # Уведомления в Telegram
│   └── main.py                 # Главный скрипт
├── downloads/                  # Скачанные файлы
│   └── [channel_name]/
│       └── [series_name]/
│           ├── [series_name].480p.mp4
│           ├── [series_name].720p.mp4
│           ├── poster.jpg
│           ├── image_1.jpg
│           └── description.txt
├── sessions/                   # Файлы сессий Telegram
├── data/                       # Метаданные
│   └── downloaded_files.json
├── logs/                       # Логи
│   └── downloader.log
├── requirements.txt
├── run.py                      # Точка входа
└── README.md
```

## 🎨 Как работает извлечение названий

Бот умно извлекает название серии из текста поста:

1. **Приоритет 1**: Текст поста (из сообщения с видео или из любого сообщения альбома)
2. **Приоритет 2**: Имя файла документа (только если это не просто "серия 1")
3. **Приоритет 3**: ID сообщения (fallback)

### Примеры обработки:

**Входной текст:**
```
Рейка — моя великолепная королева / Reika wa Karei na Boku no Joou The Animation - 1 и 2 серии
```

**Результат:**
- Папка: `Рейка — моя великолепная королева`
- Файл: `Рейка — моя великолепная королева.480p.mp4`

Если оба названия (русское и английское) влезают в лимит 150 символов - используются оба:
- Папка: `Русское название / English Title`
- Файл: `Русское название / English Title.720p.mp4`

## ⚙️ Основные настройки

### Параллелизм

```yaml
download:
  max_concurrent: 5  # Количество одновременных загрузок (рекомендуется 5-10)
```

### Качество видео

```yaml
download:
  video_qualities: [360, 480, 720]  # Какие качества скачивать
  download_nearest_quality: true     # Скачивать ближайшее меньшее, если точного нет
```

### Уведомления

```yaml
notifications:
  enabled: true
  notify_on_completion: true   # Уведомлять при завершении
  notify_on_errors: true        # Уведомлять при ошибках
  notify_on_start: false        # Уведомлять при начале
```

Подробнее о настройке скорости см. [CONFIG_GUIDE.md](CONFIG_GUIDE.md).

## 🔄 Продолжение загрузки

Бот автоматически продолжает с места остановки:

- ✅ **Метаданные сохраняются сразу** после начала загрузки
- ✅ При следующем запуске бот проверяет уже скачанные файлы
- ✅ Пропускает полные файлы, продолжает неполные
- ✅ Не нужно перезагружать бота - он сам продолжит работу

## 📊 Логирование

Логи сохраняются в:
- **Консоль** (всегда)
- **Файл** `logs/downloader.log` (если указан в конфиге)

Уровень логирования:
```yaml
logging:
  level: INFO  # DEBUG, INFO, WARNING, ERROR
```

## 🔒 Безопасность

⚠️ **Важно:**
- Не публикуйте `config.yaml` с вашими credentials
- Файл `config.yaml` уже добавлен в `.gitignore`
- Не делитесь файлами сессий (`sessions/*.session`)

## 🐛 Решение проблем

### Ошибка авторизации
- Убедитесь, что API ID и API Hash правильные
- Удалите файл сессии в `sessions/` и авторизуйтесь заново

### Канал не найден
- Проверьте, что у вас есть доступ к каналу
- Для приватных каналов убедитесь, что вы подписаны

### Ошибки загрузки
- Проверьте подключение к интернету
- Увеличьте `retry_attempts` и `retry_delay` в конфиге
- Проверьте логи в `logs/downloader.log`

### FloodWait ошибки
- Telegram ограничивает частоту запросов
- Бот автоматически ждет нужное время
- Можно уменьшить `max_concurrent` для снижения нагрузки

### Файлы скачиваются с неправильными названиями
- Убедитесь, что в посте есть текст с названием
- Проверьте логи - там будет видно, откуда взято название
- Если название в альбоме - бот автоматически найдет его

## 📝 Лицензия

Этот проект создан для личного использования.

## 💬 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/downloader.log`
2. Убедитесь, что конфигурация правильная
3. Проверьте доступ к каналам
4. Изучите [INSTALLATION.md](INSTALLATION.md) и [CONFIG_GUIDE.md](CONFIG_GUIDE.md)
