# ⚙️ Руководство по настройке конфига для скорости скачивания

Подробное руководство по оптимизации настроек для максимальной скорости загрузки видео.

## 📋 Содержание

1. [Базовые настройки скорости](#базовые-настройки-скорости)
2. [Настройка параллелизма](#настройка-параллелизма)
3. [Настройка качества видео](#настройка-качества-видео)
4. [Настройка повторных попыток](#настройка-повторных-попыток)
5. [Рекомендуемые настройки](#рекомендуемые-настройки)
6. [Мониторинг производительности](#мониторинг-производительности)
7. [Решение проблем со скоростью](#решение-проблем-со-скоростью)

---

## 🚀 Базовые настройки скорости

Откройте файл `config/config.yaml` и найдите секцию `download`:

```yaml
download:
  max_concurrent: 5          # Количество одновременных загрузок
  download_path: "./downloads"
  retry_attempts: 3           # Количество попыток при ошибке
  retry_delay: 5              # Задержка между попытками (секунды)
  video_qualities: [360, 480, 720]
  download_nearest_quality: true
```

---

## 🔄 Настройка параллелизма

### Параметр: `max_concurrent`

**Что это:** Количество файлов, которые загружаются одновременно.

**Текущее значение:** `5`

**Как изменить:**
```yaml
download:
  max_concurrent: 10  # Увеличьте с 5 до 10
```

### Рекомендации по выбору значения:

| Скорость интернета | Рекомендуемое значение | Максимальное значение |
|-------------------|----------------------|---------------------|
| < 50 Мбит/с       | 5                    | 7                   |
| 50-100 Мбит/с     | 8                    | 10                  |
| 100+ Мбит/с       | 10                   | 15                  |

### ⚠️ Важные ограничения:

1. **Не ставьте больше 15** - Telegram может заблокировать ваш аккаунт за слишком частые запросы
2. **Если видите ошибки `FloodWait`** - уменьшите значение на 2-3
3. **Оптимальное значение зависит от:**
   - Скорости вашего интернета
   - Нагрузки на серверы Telegram
   - Размера файлов

### Как найти оптимальное значение:

1. **Начните с 8:**
   ```yaml
   max_concurrent: 8
   ```

2. **Запустите бота и понаблюдайте 10-15 минут:**
   - Смотрите на прогресс-бары - сколько файлов загружается одновременно
   - Проверьте логи на наличие ошибок `FloodWait`

3. **Если нет ошибок - попробуйте увеличить:**
   ```yaml
   max_concurrent: 10
   ```

4. **Если появляются `FloodWait` - уменьшите:**
   ```yaml
   max_concurrent: 7
   ```

5. **Повторяйте до нахождения оптимального значения**

### Примеры ошибок, которые требуют уменьшения:

```
WARNING - FloodWait: нужно подождать 30 секунд
WARNING - FloodWait: нужно подождать 60 секунд
```

Если такие ошибки появляются часто (чаще 1 раза в 5 минут) - уменьшите `max_concurrent`.

---

## 🎬 Настройка качества видео

### Параметр: `video_qualities`

**Что это:** Список качеств видео, которые нужно скачивать.

**Текущее значение:** `[360, 480, 720]`

**Как изменить:**
```yaml
download:
  video_qualities: [360, 480, 720]  # Все качества
  video_qualities: [480, 720]       # Только 480p и 720p
  video_qualities: [720]             # Только 720p
  video_qualities: [360, 480]        # Только 360p и 480p (быстрее)
```

### Влияние на скорость:

| Настройка | Скорость загрузки | Размер файлов | Качество |
|----------|------------------|--------------|----------|
| `[360, 480, 720]` | Медленнее | Больше | Лучше |
| `[480, 720]` | Средняя | Средний | Хорошее |
| `[360, 480]` | Быстрее | Меньше | Хорошее |
| `[360]` | Самая быстрая | Самый маленький | Базовое |

### Рекомендации:

- **Для максимальной скорости:** `[360, 480]` - файлы меньше, загружаются быстрее
- **Для баланса:** `[480, 720]` - хорошее качество, приемлемая скорость
- **Для лучшего качества:** `[360, 480, 720]` - все доступные качества

### Параметр: `download_nearest_quality`

**Что это:** Если точного качества нет, скачивать ближайшее меньшее?

**Текущее значение:** `true`

**Рекомендация:** Оставьте `true` - это позволит скачать видео, даже если точного качества нет.

```yaml
download:
  download_nearest_quality: true  # Рекомендуется
```

---

## 🔁 Настройка повторных попыток

### Параметр: `retry_attempts`

**Что это:** Количество попыток загрузки файла при ошибке.

**Текущее значение:** `3`

**Как изменить:**
```yaml
download:
  retry_attempts: 5  # Увеличьте для нестабильного интернета
```

**Рекомендации:**
- **Стабильный интернет:** `3` (по умолчанию)
- **Нестабильный интернет:** `5`
- **Очень нестабильный:** `7` (но не больше)

### Параметр: `retry_delay`

**Что это:** Задержка между попытками в секундах.

**Текущее значение:** `5`

**Как изменить:**
```yaml
download:
  retry_delay: 3  # Уменьшите для быстрого интернета
  retry_delay: 10 # Увеличьте для медленного интернета
```

**Рекомендации:**
- **Быстрый интернет (100+ Мбит/с):** `3` секунды
- **Средний интернет (50-100 Мбит/с):** `5` секунд (по умолчанию)
- **Медленный интернет (<50 Мбит/с):** `7-10` секунд

---

## 📊 Рекомендуемые настройки

### Для быстрого интернета (100+ Мбит/с)

```yaml
download:
  max_concurrent: 10
  retry_attempts: 3
  retry_delay: 3
  video_qualities: [360, 480, 720]
  download_nearest_quality: true
```

**Ожидаемая скорость:** Максимальная скорость вашего интернета

### Для среднего интернета (50-100 Мбит/с)

```yaml
download:
  max_concurrent: 8
  retry_attempts: 3
  retry_delay: 5
  video_qualities: [360, 480, 720]
  download_nearest_quality: true
```

**Ожидаемая скорость:** 30-70 Мбит/с

### Для медленного интернета (<50 Мбит/с)

```yaml
download:
  max_concurrent: 5
  retry_attempts: 5
  retry_delay: 7
  video_qualities: [360, 480]  # Убрали 720p для экономии трафика
  download_nearest_quality: true
```

**Ожидаемая скорость:** 10-40 Мбит/с

### Для максимальной скорости (жертвуя качеством)

```yaml
download:
  max_concurrent: 10
  retry_attempts: 3
  retry_delay: 3
  video_qualities: [360, 480]  # Только меньшее качество
  download_nearest_quality: true
```

**Ожидаемая скорость:** Максимальная, но файлы будут меньше

---

## 📈 Мониторинг производительности

### Как проверить текущую скорость:

1. **Смотрите на прогресс-бары в консоли:**
   ```
   video1.720p.mp4: 45%|████▌     | 450MB/1GB [2:30<00:03, 3.2MB/s]
   ```
   Здесь `3.2MB/s` - это скорость загрузки одного файла

2. **Проверьте логи:**
   ```bash
   # Windows
   Get-Content logs/downloader.log -Tail 50
   
   # Linux/macOS
   tail -n 50 logs/downloader.log
   ```

3. **Оцените общую скорость:**
   - Если `max_concurrent: 5` и каждый файл загружается со скоростью 3 MB/s
   - Общая скорость ≈ 5 × 3 MB/s = 15 MB/s = 120 Мбит/с

### Что смотреть в логах:

**Хорошие признаки:**
```
INFO - ✓ Скачано: video1.720p.mp4 (1.2 GB)
INFO - Загрузка из Channel: 50%|█████     | 25/50 [5:20<00:05, 12.8s/файл]
```

**Плохие признаки:**
```
WARNING - FloodWait: нужно подождать 30 секунд
WARNING - Попытка 1/3 не удалась для 12345: Connection timeout
ERROR - Не удалось скачать 12345 после 3 попыток
```

---

## 🔧 Решение проблем со скоростью

### Проблема: Скорость очень низкая (<5 Мбит/с)

**Возможные причины и решения:**

1. **Низкая скорость интернета:**
   - Проверьте скорость на speedtest.net
   - Если скорость низкая - это нормально, увеличьте `retry_delay`

2. **Слишком много одновременных загрузок:**
   ```yaml
   download:
     max_concurrent: 3  # Уменьшите
   ```

3. **Проблемы с серверами Telegram:**
   - Попробуйте запустить позже
   - Используйте VPN (см. ниже)

### Проблема: Частые ошибки FloodWait

**Решение:**
```yaml
download:
  max_concurrent: 5  # Уменьшите на 2-3 от текущего значения
```

### Проблема: Файлы загружаются, но очень медленно

**Попробуйте:**

1. **Увеличить параллелизм:**
   ```yaml
   download:
     max_concurrent: 8  # Если было 5
   ```

2. **Использовать VPN:**
   - Выберите сервер близко к серверам Telegram (обычно Европа)
   - Используйте быстрый VPN (WireGuard протокол)

3. **Проверить другие программы:**
   - Закройте программы, которые используют интернет
   - Используйте проводное подключение вместо Wi-Fi

### Проблема: Скорость хорошая, но хочется быстрее

**Оптимизация:**

1. **Увеличьте `max_concurrent` до 10-12:**
   ```yaml
   download:
     max_concurrent: 10
   ```

2. **Уменьшите `retry_delay`:**
   ```yaml
   download:
     retry_delay: 3
   ```

3. **Скачивайте только нужные качества:**
   ```yaml
   download:
     video_qualities: [480, 720]  # Убрали 360p
   ```

---

## 💡 Продвинутые советы

### Использование VPN

**Когда VPN поможет:**
- ✅ Если ваш провайдер ограничивает скорость до Telegram
- ✅ Если Telegram заблокирован в вашей стране
- ✅ Если есть проблемы с маршрутизацией

**Когда VPN НЕ поможет:**
- ❌ Если ограничение от самого Telegram API
- ❌ Если проблема в скорости вашего интернета

**Рекомендации:**
- Выбирайте серверы близко к серверам Telegram (Европа)
- Используйте быстрые VPN (WireGuard протокол)
- Избегайте бесплатных VPN

### Оптимизация сети

1. **Используйте проводное подключение** вместо Wi-Fi
2. **Закройте другие программы**, которые используют интернет
3. **Проверьте настройки Windows:**
   - Отключите ограничения скорости в настройках сети
   - Проверьте, нет ли QoS (Quality of Service) ограничений

### Мониторинг в реальном времени

Используйте прогресс-бары для оценки:
- **Скорость каждого файла** (MB/s) - видно в прогресс-баре
- **Общее количество активных загрузок** - должно быть равно `max_concurrent`
- **Частота ошибок** - смотрите в логах

---

## ⚠️ Ограничения Telegram API

Telegram имеет встроенные ограничения:

- **Rate limiting** - ограничение частоты запросов
- **FloodWait** - при превышении лимита нужно ждать
- **Скорость серверов** - зависит от нагрузки на серверы Telegram

**Что делать при FloodWait:**
- Бот автоматически ждет нужное время
- Это нормально, не паникуйте
- Если FloodWait очень частые - уменьшите `max_concurrent`

---

## ✅ Чек-лист оптимизации

- [ ] Определил скорость интернета
- [ ] Выбрал подходящее значение `max_concurrent`
- [ ] Настроил `video_qualities` под свои нужды
- [ ] Настроил `retry_attempts` и `retry_delay`
- [ ] Запустил бота и проверил скорость
- [ ] Проверил логи на наличие ошибок
- [ ] При необходимости скорректировал настройки

---

## 🎯 Быстрая настройка (TL;DR)

**Для максимальной скорости:**

1. Откройте `config/config.yaml`
2. Измените:
   ```yaml
   download:
     max_concurrent: 10  # Увеличьте с 5 до 10
     retry_delay: 3      # Уменьшите с 5 до 3 (если интернет быстрый)
   ```
3. Сохраните файл
4. Запустите бота - он автоматически продолжит с места остановки!

**Важно:** Не ставьте `max_concurrent` больше 15, иначе Telegram может заблокировать аккаунт!

---

## 📚 Дополнительная информация

- **[README.md](README.md)** - Общее описание проекта
- **[INSTALLATION.md](INSTALLATION.md)** - Инструкция по установке

---

**Готово!** Теперь вы знаете, как оптимизировать скорость загрузки. Экспериментируйте с настройками и найдите оптимальные значения для вашего случая.

