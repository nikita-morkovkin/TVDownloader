# 🚀 Оптимизация скорости скачивания видео

## 📊 Текущие настройки

В вашем `config.yaml`:

- `max_concurrent: 5` - загружается 5 файлов одновременно
- Это хорошее значение по умолчанию, но можно оптимизировать

## ⚡ Способы ускорения загрузки

### 1. **Увеличить количество одновременных загрузок**

**Рекомендация:** Увеличьте `max_concurrent` до 8-10

```yaml
download:
  max_concurrent: 10 # Было 5, стало 10
```

**⚠️ Важно:**

- **Не ставьте больше 15** - Telegram может заблокировать ваш аккаунт за слишком частые запросы
- Если видите ошибки `FloodWait`, уменьшите значение
- Оптимальное значение зависит от вашего интернета и серверов Telegram

**Как найти оптимальное значение:**

1. Начните с 8
2. Если нет ошибок - попробуйте 10
3. Если появляются `FloodWait` - уменьшите до 7-8

### 2. **Использование VPN**

**Когда VPN поможет:**

- ✅ Если ваш провайдер ограничивает скорость до Telegram серверов
- ✅ Если Telegram заблокирован в вашей стране
- ✅ Если есть проблемы с маршрутизацией до серверов Telegram

**Когда VPN НЕ поможет:**

- ❌ Если ограничение идет от самого Telegram API (лимиты на аккаунт)
- ❌ Если проблема в скорости вашего интернета
- ❌ Если серверы Telegram перегружены

**Рекомендации по VPN:**

- Выбирайте серверы **близко к серверам Telegram** (обычно в Европе)
- Используйте **быстрые VPN** (WireGuard протокол предпочтительнее)
- Избегайте бесплатных VPN - они часто медленнее

**Как проверить, поможет ли VPN:**

1. Запустите загрузку без VPN, засеките скорость
2. Запустите с VPN, сравните скорость
3. Если разница > 20% - VPN помогает

### 3. **Оптимизация качества видео**

Если скорость критична, можно скачивать только меньшее качество:

```yaml
download:
  video_qualities: [360, 480] # Убрали 720p - файлы будут меньше
```

**Плюсы:** Быстрее загрузка, меньше трафик
**Минусы:** Меньше качество видео

### 4. **Оптимизация сети**

- Используйте **проводное подключение** вместо Wi-Fi
- Закройте другие программы, которые используют интернет
- Проверьте, нет ли ограничений скорости в настройках Windows

## 🔄 Нужно ли перезагружать бота?

### ❌ НЕТ, перезагружать НЕ нужно!

**Почему:**

- Бот **автоматически пропускает** уже скачанные файлы
- При запуске он проверяет метаданные в `data/downloaded_files.json`
- Уже скачанные файлы не будут скачиваться повторно

**Как это работает:**

```python
# В коде есть проверка:
if self.file_handler.is_file_downloaded(message_id, channel_name):
    logger.debug(f"Файл {message_id} уже скачан, пропускаем")
    self.skipped_count += 1
    return False
```

**Что произойдет при перезапуске:**

1. Бот подключится к Telegram
2. Проверит все файлы в канале
3. **Пропустит** уже скачанные (покажет "skipped")
4. **Продолжит** скачивание с места остановки

**Когда перезапуск полезен:**

- Если бот завис или произошла ошибка
- Если вы изменили настройки в `config.yaml`
- Если хотите проверить, что все работает

## 🗑️ Нужно ли удалять уже скачанные файлы?

### ❌ НЕТ, удалять НЕ нужно!

**Почему:**

- Бот не будет скачивать их повторно
- Удаление файлов **не ускорит** загрузку
- Метаданные хранятся отдельно в `data/downloaded_files.json`

**Что происходит:**

1. Бот проверяет `data/downloaded_files.json` (НЕ папку downloads)
2. Если файл есть в метаданных - пропускает
3. Если файла нет в метаданных - скачивает

**⚠️ ВАЖНО:** Если вы удалите файлы из папки `downloads/`, но НЕ удалите запись из `data/downloaded_files.json`, бот будет думать, что файл уже скачан и пропустит его!

**Если хотите перескачать файлы:**

1. Удалите запись из `data/downloaded_files.json` (или весь файл)
2. Или удалите конкретную запись для канала

## 📈 Рекомендуемые настройки для максимальной скорости

### Для быстрого интернета (100+ Мбит/с):

```yaml
download:
  max_concurrent: 10
  retry_attempts: 3
  retry_delay: 3 # Уменьшили с 5 до 3
```

### Для среднего интернета (50-100 Мбит/с):

```yaml
download:
  max_concurrent: 8
  retry_attempts: 3
  retry_delay: 5
```

### Для медленного интернета (<50 Мбит/с):

```yaml
download:
  max_concurrent: 5 # Оставляем как есть
  retry_attempts: 3
  retry_delay: 5
```

## 🧪 Как протестировать настройки

1. **Измените `max_concurrent` в `config.yaml`**
2. **Запустите бота** и понаблюдайте 5-10 минут
3. **Проверьте логи** на наличие ошибок:
   ```bash
   # Windows
   Get-Content logs/downloader.log -Tail 50
   ```
4. **Оцените скорость** по прогресс-барам
5. **Если есть `FloodWait`** - уменьшите `max_concurrent`

## 📊 Мониторинг производительности

Используйте прогресс-бары для оценки:

- **Скорость каждого файла** (MB/s) - видно в прогресс-баре
- **Общее количество активных загрузок** - должно быть равно `max_concurrent`
- **Частота ошибок** - смотрите в логах

## ⚠️ Ограничения Telegram API

Telegram имеет встроенные ограничения:

- **Rate limiting** - ограничение частоты запросов
- **FloodWait** - при превышении лимита нужно ждать
- **Скорость серверов** - зависит от нагрузки на серверы Telegram

**Что делать при FloodWait:**

- Бот автоматически ждет нужное время
- Это нормально, не паникуйте
- Если FloodWait очень частые - уменьшите `max_concurrent`

## 💡 Итоговые рекомендации

1. ✅ **Увеличьте `max_concurrent` до 8-10** (если интернет позволяет)
2. ✅ **Попробуйте VPN** (если скорость низкая)
3. ✅ **НЕ перезагружайте бота** - он продолжит с места остановки
4. ✅ **НЕ удаляйте файлы** - бот их пропустит автоматически
5. ✅ **Мониторьте логи** на наличие ошибок
6. ✅ **Используйте прогресс-бары** для оценки скорости

## 🔧 Быстрая настройка

Откройте `config/config.yaml` и измените:

```yaml
download:
  max_concurrent: 10 # Увеличьте с 5 до 10
```

Сохраните и запустите бота снова. Он автоматически продолжит загрузку с места остановки!
