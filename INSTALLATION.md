# 📦 Инструкция по установке и запуску

Подробное руководство для установки и запуска TV Downloader на новом компьютере.

## 📋 Содержание

1. [Требования](#требования)
2. [Установка Python](#установка-python)
3. [Установка проекта](#установка-проекта)
4. [Получение API credentials](#получение-api-credentials)
5. [Настройка конфигурации](#настройка-конфигурации)
6. [Первый запуск](#первый-запуск)
7. [Проверка работы](#проверка-работы)
8. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

Перед началом убедитесь, что у вас есть:

- ✅ Компьютер с Windows, Linux или macOS
- ✅ Интернет-соединение
- ✅ Telegram аккаунт
- ✅ Номер телефона для авторизации в Telegram

---

## 🐍 Установка Python

### Windows

1. **Скачайте Python:**

   - Перейдите на https://www.python.org/downloads/
   - Скачайте последнюю версию Python 3.8 или выше
   - **Важно:** При установке отметьте галочку "Add Python to PATH"

2. **Проверьте установку:**
   Откройте командную строку (Win+R → `cmd`) и выполните:
   ```bash
   python --version
   ```
   Должно показать версию Python (например, `Python 3.11.0`)

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install python3 python3-pip
```

### macOS

```bash
# Если установлен Homebrew
brew install python3

# Или скачайте с python.org
```

---

## 📥 Установка проекта

### Вариант 1: Через Git (если проект в репозитории)

```bash
git clone <URL_репозитория>
cd TVDownloader
```

### Вариант 2: Скачать архив

1. Скачайте архив проекта
2. Распакуйте в любую папку (например, `C:\Projects\TVDownloader` или `~/Projects/TVDownloader`)

### Установка зависимостей

Откройте терминал/командную строку в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

**Если команда не работает, попробуйте:**

```bash
python -m pip install -r requirements.txt
```

или

```bash
python3 -m pip install -r requirements.txt
```

**Ожидаемый результат:**

```
Successfully installed telethon-1.34.0 pyyaml-6.0.1 tqdm-4.66.1 ...
```

---

## 🔑 Получение API credentials

Для работы бота нужны API ID и API Hash от Telegram.

### Шаг 1: Перейдите на сайт Telegram

1. Откройте https://my.telegram.org/apps
2. Войдите с вашим номером телефона
3. Введите код подтверждения из Telegram

### Шаг 2: Создайте приложение

1. Если приложение еще не создано, нажмите "Create application"
2. Заполните форму:

   - **App title:** Любое название (например, "TV Downloader")
   - **Short name:** Любое короткое имя (например, "tvdownloader")
   - **Platform:** Desktop
   - **Description:** Можно оставить пустым

3. Нажмите "Create"

### Шаг 3: Скопируйте credentials

После создания вы увидите:

- **api_id** - число (например, `12345678`)
- **api_hash** - строка (например, `abcdef1234567890abcdef1234567890`)

**Сохраните эти данные!** Они понадобятся в следующем шаге.

---

## ⚙️ Настройка конфигурации

### Шаг 1: Создайте файл конфигурации

1. Найдите файл `config/config.example.yaml` в проекте
2. Скопируйте его и переименуйте в `config/config.yaml`

**Windows:**

```bash
copy config\config.example.yaml config\config.yaml
```

**Linux/macOS:**

```bash
cp config/config.example.yaml config/config.yaml
```

### Шаг 2: Откройте файл для редактирования

Откройте `config/config.yaml` в любом текстовом редакторе (Блокнот, VS Code, Notepad++ и т.д.)

### Шаг 3: Заполните базовые настройки

```yaml
telegram:
  api_id: 12345678 # Вставьте ваш api_id
  api_hash: "abcdef1234..." # Вставьте ваш api_hash
  channels:
    - "@channel_name" # Или invite link, или ID канала
```

### Шаг 4: Добавьте каналы для скачивания

Вы можете указать каналы в трех форматах:

**1. Username (публичный канал):**

```yaml
channels:
  - "@channel_name"
```

**2. Invite link (приватный канал):**

```yaml
channels:
  - "https://t.me/joinchat/AAAAAFZTTF99JzOWLZH74A"
```

**3. Channel ID:**

```yaml
channels:
  - -1001234567890
```

**Как найти invite link:**

- Откройте канал в Telegram
- Нажмите на название канала
- Выберите "Add members" или "Invite to Group"
- Скопируйте ссылку

**Можно указать несколько каналов:**

```yaml
channels:
  - "@channel1"
  - "@channel2"
  - "https://t.me/joinchat/..."
```

### Шаг 5: Настройте уведомления (опционально)

Если хотите получать уведомления в Telegram:

1. **Создайте бота:**

   - Найдите @BotFather в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Получите Chat ID:**

   - Напишите вашему боту любое сообщение
   - Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Найдите `"chat":{"id":123456789}` - это ваш Chat ID

3. **Добавьте в конфиг:**

```yaml
notifications:
  enabled: true
  bot_token: "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
  chat_id: 123456789
  notify_on_completion: true
  notify_on_errors: true
  notify_on_start: false
```

Если уведомления не нужны:

```yaml
notifications:
  enabled: false
```

### Шаг 6: Сохраните файл

Сохраните `config/config.yaml` с вашими настройками.

---

## 🚀 Первый запуск

### Windows

**Вариант 1: Через командную строку**

```bash
cd C:\Projects\TVDownloader  # Путь к вашему проекту
python run.py
```

**Вариант 2: Через двойной клик**

- Дважды кликните на файл `INSTALL_AND_RUN.bat`

### Linux/macOS

```bash
cd ~/Projects/TVDownloader  # Путь к вашему проекту
python3 run.py
```

### Что произойдет при первом запуске:

1. **Подключение к Telegram:**

   ```
   Успешно подключено к Telegram
   ```

2. **Запрос номера телефона:**

   ```
   Введите номер телефона:
   ```

   Введите номер с кодом страны (например: `+79991234567`)

3. **Запрос кода подтверждения:**

   ```
   Введите код подтверждения:
   ```

   - Откройте Telegram на телефоне
   - Найдите сообщение с кодом
   - Введите код в консоль

4. **Сохранение сессии:**
   После успешной авторизации сессия сохранится в `sessions/tvdownloader.session`

   - При следующих запусках код больше не понадобится

5. **Начало скачивания:**
   ```
   Обработка канала: @channel_name
   Имя канала: Channel Name
   Найдено 50 видео в канале Channel Name
   Загрузка из Channel Name: 100%|████████| 50/50 [10:30<00:00, 12.6s/файл]
   ```

---

## ✅ Проверка работы

### 1. Проверьте логи

Откройте `logs/downloader.log` - там должна быть информация о работе бота.

### 2. Проверьте скачанные файлы

Файлы должны появиться в папке `downloads/`:

```
downloads/
└── [Имя канала]/
    └── [Название серии]/
        ├── [Название серии].480p.mp4
        ├── [Название серии].720p.mp4
        ├── poster.jpg
        ├── image_1.jpg
        └── description.txt
```

### 3. Проверьте метаданные

Файл `data/downloaded_files.json` должен содержать информацию о скачанных файлах.

---

## 🐛 Решение проблем

### Проблема: "python не является внутренней или внешней командой"

**Решение:**

- Убедитесь, что Python установлен
- При установке Python должна быть отмечена галочка "Add Python to PATH"
- Перезапустите командную строку после установки Python
- Попробуйте использовать `py` вместо `python`:
  ```bash
  py run.py
  ```

### Проблема: "ModuleNotFoundError: No module named 'telethon'"

**Решение:**

```bash
pip install -r requirements.txt
```

Если не помогает:

```bash
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

### Проблема: "Файл конфигурации не найден"

**Решение:**

- Убедитесь, что файл `config/config.yaml` существует
- Проверьте, что вы запускаете скрипт из корневой папки проекта
- Убедитесь, что файл называется именно `config.yaml` (не `config.example.yaml`)

### Проблема: "Ошибка авторизации" или "Invalid API ID"

**Решение:**

- Проверьте, что `api_id` и `api_hash` скопированы правильно
- Убедитесь, что `api_id` - это число (без кавычек)
- Убедитесь, что `api_hash` - это строка (в кавычках)
- Удалите папку `sessions/` и авторизуйтесь заново

### Проблема: "Канал не найден" или "ChannelPrivateError"

**Решение:**

- Убедитесь, что у вас есть доступ к каналу
- Для приватных каналов убедитесь, что вы подписаны
- Проверьте правильность invite link или username
- Попробуйте использовать invite link вместо username

### Проблема: "FloodWait: нужно подождать X секунд"

**Решение:**

- Это нормально - Telegram ограничивает частоту запросов
- Бот автоматически подождет нужное время
- Если ошибки частые, уменьшите `max_concurrent` в конфиге

### Проблема: Файлы скачиваются медленно

**Решение:**

- См. [CONFIG_GUIDE.md](CONFIG_GUIDE.md) для оптимизации скорости
- Увеличьте `max_concurrent` в конфиге (но не больше 10-15)
- Проверьте скорость интернета

### Проблема: Бот не продолжает с места остановки

**Решение:**

- Убедитесь, что файл `data/downloaded_files.json` существует
- Проверьте логи - там будет видно, почему файлы пропускаются или скачиваются заново
- Если файл неполный (прервалась загрузка), бот автоматически удалит его и начнет заново

---

## 📚 Дополнительная информация

- **[README.md](README.md)** - Общее описание проекта и возможностей
- **[CONFIG_GUIDE.md](CONFIG_GUIDE.md)** - Руководство по настройке скорости скачивания

---

## ✅ Готово!

Если все шаги выполнены успешно, бот должен работать. При следующих запусках просто выполните:

```bash
python run.py
```

Бот автоматически продолжит загрузку с места остановки и не будет скачивать уже загруженные файлы.
