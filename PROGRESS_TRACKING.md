# Отслеживание прогресса скачивания видео

## 📊 Способы проверки прогресса

В проекте реализовано несколько способов отслеживания прогресса скачивания видео:

### 1. **Детальные прогресс-бары для каждого файла** (новое!)

При запуске скачивания вы увидите:
- **Прогресс-бар для каждого файла** с информацией:
  - Имя файла (первые 50 символов)
  - Процент загрузки
  - Текущий размер / Общий размер
  - Скорость загрузки (MB/s, KB/s и т.д.)

Пример вывода:
```
1 серия_720p.mp4: 45%|████████████          | 450MB/1.0GB [2.5MB/s]
2 серия_720p.mp4: 78%|██████████████████   | 780MB/1.0GB [3.2MB/s]
3 серия_720p.mp4: 12%|███                  | 120MB/1.0GB [1.8MB/s]
```

### 2. **Общий прогресс по количеству файлов**

Вверху экрана отображается общий прогресс:
```
Загрузка из Full Hentai 18+: 45%|████████      | 742/1656 файл
```

### 3. **Логи в консоли**

В консоли выводятся сообщения о:
- Начале загрузки каждого файла
- Успешном завершении загрузки с размером файла
- Ошибках и повторных попытках

Пример:
```
2025-11-28 22:33:39 - INFO - ✓ Скачано: 1 серия_720p.mp4 (1.2 GB)
2025-11-28 22:33:45 - INFO - ✓ Скачано: 2 серия_720p.mp4 (980.5 MB)
```

### 4. **Логи в файле**

Все события записываются в файл `logs/downloader.log`:
```bash
# Просмотр логов в реальном времени (Linux/Mac)
tail -f logs/downloader.log

# Просмотр последних 50 строк (Windows)
powershell Get-Content logs/downloader.log -Tail 50
```

### 5. **Проверка файлов в папке downloads**

Вы можете проверить прогресс, посмотрев на файлы в папке `downloads/`:
```
downloads/
└── Full Hentai 18+/
    ├── 1 серия_720p.mp4  (1.2 GB) ✓ Завершено
    ├── 2 серия_720p.mp4  (980 MB)  ✓ Завершено
    └── 3 серия_720p.mp4  (450 MB)  ⏳ В процессе
```

**Важно:** Если файл еще скачивается, его размер будет меньше финального размера.

### 6. **Метаданные в data/downloaded_files.json**

После завершения загрузки каждого файла информация сохраняется в JSON:
```json
{
  "Full Hentai 18+": {
    "total_files": 1656,
    "total_size": 1234567890,
    "last_updated": "2025-11-28T22:33:45",
    "messages": {
      "12345": {
        "file_path": "downloads/Full Hentai 18+/1 серия_720p.mp4",
        "file_size": 1234567890,
        "quality": 720,
        "downloaded_at": "2025-11-28T22:33:45"
      }
    }
  }
}
```

## 🔍 Как проверить прогресс в реальном времени

### Способ 1: Смотреть в консоль

Просто запустите программу и смотрите на прогресс-бары в консоли:
```bash
python -m src.main
```

### Способ 2: Мониторить логи

В отдельном терминале запустите:
```bash
# Linux/Mac
tail -f logs/downloader.log

# Windows PowerShell
Get-Content logs/downloader.log -Wait -Tail 20
```

### Способ 3: Проверить размер файлов

Используйте команду для проверки размера файлов:
```bash
# Linux/Mac
du -sh downloads/*/

# Windows PowerShell
Get-ChildItem downloads -Recurse | Measure-Object -Property Length -Sum
```

### Способ 4: Создать скрипт для мониторинга

Создайте файл `check_progress.py`:
```python
import json
from pathlib import Path
from datetime import datetime

def check_progress():
    """Проверка текущего прогресса загрузки."""
    metadata_file = Path("data/downloaded_files.json")
    
    if not metadata_file.exists():
        print("Метаданные не найдены. Загрузка еще не началась.")
        return
    
    with open(metadata_file, 'r', encoding='utf-8') as f:
        metadata = json.load(f)
    
    print("=" * 60)
    print("ТЕКУЩИЙ ПРОГРЕСС ЗАГРУЗКИ")
    print("=" * 60)
    
    for channel_name, channel_data in metadata.items():
        total_files = channel_data.get('total_files', 0)
        total_size = channel_data.get('total_size', 0)
        last_updated = channel_data.get('last_updated', 'N/A')
        
        # Форматируем размер
        size_gb = total_size / (1024 ** 3)
        
        print(f"\n📺 Канал: {channel_name}")
        print(f"   Скачано файлов: {total_files}")
        print(f"   Общий размер: {size_gb:.2f} GB")
        print(f"   Последнее обновление: {last_updated}")
    
    print("=" * 60)

if __name__ == "__main__":
    check_progress()
```

Запустите:
```bash
python check_progress.py
```

## 📈 Итоговая статистика

После завершения всех загрузок вы увидите итоговую статистику:
```
==================================================
ИТОГОВАЯ СТАТИСТИКА:
Скачано: 1656
Пропущено: 0
Ошибок: 0
Общий размер: 1.23 TB
==================================================
```

## ⚙️ Настройка уровня детализации

В файле `config/config.yaml` можно настроить уровень логирования:

```yaml
logging:
  level: INFO  # DEBUG для более детальной информации
```

- **INFO** - показывает основные события (рекомендуется)
- **DEBUG** - показывает все детали, включая отладочную информацию

## 💡 Советы

1. **Для больших загрузок**: Используйте `tail -f` для мониторинга логов в реальном времени
2. **Для проверки конкретного файла**: Найдите его в папке `downloads/` и проверьте размер
3. **При прерывании**: Программа автоматически пропустит уже скачанные файлы при следующем запуске
4. **Скорость загрузки**: Зависит от вашего интернет-соединения и ограничений Telegram API

## 🐛 Решение проблем

### Прогресс-бары не отображаются
- Убедитесь, что терминал поддерживает ANSI коды
- Попробуйте запустить в другом терминале (Git Bash, PowerShell, CMD)

### Неточная информация о размере
- Размер обновляется после завершения загрузки каждого файла
- Для файлов в процессе размер может быть меньше финального

### Логи не обновляются
- Проверьте, что файл `logs/downloader.log` существует
- Убедитесь, что у программы есть права на запись в папку `logs/`

